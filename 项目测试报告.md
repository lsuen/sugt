# SUGT 项目测试报告

> 测试日期：2026-06-10  
> 项目目录：`D:\CodeFiles\RustProjects\sugt`  
> 项目类型：Tauri 2 + Rust + React/TypeScript + Vite + Axum 的本地大模型网关  
> 测试目标：基于代码分析编写测试点，复制为执行记录，按测试点执行自动化/冒烟验证，并形成原功能、业务、界面、安全、稳定性和优化建议报告。

## 1. 项目与业务概述

SUGT（su gateway）定位为内部研发人员使用的本地大模型网关。它通过本机 GUI 与 CLI 管理上游模型服务配置，并向 Claude Code、Codex 等开发客户端暴露 OpenAI/Anthropic 兼容代理接口，使国内或上游模型服务能够被统一接入。

核心功能包括：

1. **本地网关服务**：通过 Axum 提供 `/health`、`/v1/models`、`/v1/messages` 和通配代理路径。
2. **模型配置管理**：支持 OpenAI/Anthropic 协议、服务商预设、Base URL 规范化、API Key 脱敏展示、启停、默认模型标识/切换和一键测试。
3. **协议适配**：支持 Anthropic Messages 与 OpenAI Chat Completions 的请求/响应转换，包含工具调用和 SSE 流式响应适配。
4. **客户端接管**：为 Claude/Codex 写入或输出本地网关环境变量，客户端使用 dummy key 访问本机网关，真实上游 Key 仅保存在 SUGT 配置中。
5. **桌面 GUI**：提供控制台、模型配置、客户端接管、运行日志、关于等页面，关于页低调展示内部试用/自用状态。
6. **CLI**：提供初始化、状态查看、启动服务、连接测试、环境变量状态/安装/打印/卸载等命令。
7. **内部试用治理**：通过编译期元数据写入 trial/self 版本信息，试用版首次运行绑定机器，到期后禁止网关相关能力，自用版无有效期限制。
8. **系统托盘增强**：托盘菜单展示服务、模型、接管、版本状态，并提供启动/停止网关、接管开关、打开配置目录等快捷操作。
9. **portable 打包**：生成 Windows 便携目录和可选 zip 包，支持 trial/self 多版本打包。

## 2. 测试范围

### 2.1 已覆盖范围

| 范围 | 覆盖方式 | 结论 |
| --- | --- | --- |
| TypeScript 类型检查 | `npm run check` | 通过 |
| 前端生产构建 | `npm run build` | 通过 |
| Rust 单元测试 | `cargo test` | 14 个测试通过 |
| portable 打包 | `npm run package:release` | 历史通过；本轮默认改为 self 包，待复测 |
| trial/self 打包入口 | `scripts/package-release.ps1 -Variant trial/self`、`package:release:*` | 待复测 |
| zip 组包逻辑 | `scripts/package-release.ps1 -SkipBuild -Zip` | 历史通过；新目录命名待复测 |
| npm zip 完整入口 | `npm run package:release:zip` | 历史验证中构建通过，清理旧产物时被运行中的 `SUGT.exe` 锁定；本轮入口已改为 self，待复测 |
| CLI 初始化/状态/env | release `sugt-cli.exe` + 临时配置目录 | 通过 |
| 网关健康检查 | release `sugt-cli.exe serve` + `/health` | 通过 |
| 模型列表接口 | release `sugt-cli.exe serve` + `/v1/models` | 通过 |
| 协议适配 | Rust 单元测试 + 代码检查 | 通过 |
| GUI 功能逻辑 | 代码检查 + 打包产物验证 | 未发现代码级阻塞问题 |
| 安全脱敏 | CLI 输出 + 代码检查 | 通过 |

### 2.2 未做破坏性/外部依赖验证的范围

| 范围 | 未自动执行原因 | 建议验证方式 |
| --- | --- | --- |
| 一键接管 `setx` | 会修改用户真实环境变量 | 使用隔离 Windows 测试账号或虚拟机 |
| 开机启动开关 | 会修改用户系统启动项/注册表 | 使用隔离 Windows 测试账号或虚拟机 |
| 真实上游模型请求 | 需要真实 API Key，可能产生费用或泄露风险 | 由用户提供临时 Key，在隔离配置目录验证 |
| GUI 点击级 E2E | 当前项目未配置 Tauri Driver/Playwright | 后续补充自动化 E2E |
| 强行结束运行中的 `SUGT.exe` 后复测 zip | 当前存在用户运行进程，未擅自终止 | 用户关闭 GUI 后再执行完整 zip 命令 |

## 3. 原功能测试情况

### 3.1 控制台功能

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| 服务状态展示 | `get_status` 命令返回运行状态、监听地址、当前模型、配置目录、日志文件；GUI 绑定展示 | 通过 |
| 启动网关 | CLI release 产物启动 `serve --host 127.0.0.1 --port 18787`，健康检查通过 | 通过 |
| 停止网关 | GUI 命令层存在 `stop_gateway`；测试脚本通过 finally 停止临时进程 | 通过/需 GUI 点击复测 |
| 配置目录打开 | `open_config_dir` 按平台打开配置目录 | 代码级通过 |
| 开机启动 | `set_autostart` 逻辑存在 | 需隔离系统人工验证 |
| 故障转移 | Rust 单测验证 provider 排序；代理层 5xx 重试、4xx 不重试 | 通过 |

### 3.2 模型配置功能

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| 新增模型 | 表单字段完整，命令层校验必填字段和新增 API Key | 通过 |
| 编辑模型 | 空 Key/脱敏 Key 不覆盖原真实 Key | 通过 |
| 删除模型 | 删除当前 active provider 后自动切换到下一个 enabled provider | 通过 |
| 设为默认模型 | 仅允许启用模型设为默认，前端显示“默认使用”标识和“设为默认/已默认”按钮 | 通过 |
| 列表级启用/禁用 | 新增快捷启用/禁用按钮，禁用卡片置灰；禁用当前默认模型时自动切换 | 通过 |
| 一键测试全部 | 顺序调用 `test_provider`，汇总显示可用/不可用数量 | 通过/需真实 Key 验证结果准确性 |
| OpenAI URL 规范化 | `https://example.com` 规范化为 `https://example.com/v1` | 通过 |
| Anthropic URL 规范化 | 不强制追加 `/v1` | 通过 |
| API Key 脱敏 | `test-secret-123456` 显示为 `test****3456` | 通过 |
| 连接测试 | 需要真实上游 Key，本轮未执行真实请求 | 需人工确认 |

### 3.3 客户端接管功能

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| 接管状态展示 | release `sugt-cli env status` 可显示 Claude/Codex configured=false 和 missing 变量 | 通过 |
| 环境变量脚本输出 | release `sugt-cli env print` 输出本地网关变量与 dummy key | 通过 |
| 一键接管 | 代码会写启动脚本并调用 `setx` | 需隔离系统人工验证 |
| 真实 Key 隔离 | 客户端环境变量使用 `sugt-local-key`，不使用上游 API Key | 通过 |

### 3.4 日志和关于功能

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| 日志读取 | `read_logs` 限制最多 500 行，前端默认读取 200 行 | 通过 |
| 日志为空态 | 无日志时显示“暂无日志” | 通过 |
| 日志滚动 | CSS 设置 `overflow: auto` | 通过 |
| 关于信息 | 展示产品、作者、配置目录、日志文件和低调试用状态 | 待打包冒烟确认 trial/self 文案 |

### 3.5 内部试用治理与托盘

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| 试用版有效期 | 编译期读取 `SUGT_TRIAL_EXPIRES_AT`，关于页展示有效期，到期后禁止网关启动 | 待 trial 包验证 |
| 自用版无有效期 | 编译期 `SUGT_TRIAL_ENABLED=false`，关于页展示无有效期限制 | 待 self 包验证 |
| 首次运行机器绑定 | 运行状态文件写入 `Documents/.sugt/trial-state.json` 或 `SUGT_CONFIG_DIR` | 待首次运行验证 |
| exe 目录不落运行时文件 | 状态文件路径不指向 exe 所在目录 | 待 portable 目录检查 |
| 托盘状态与操作 | 菜单展示服务/模型/接管/版本状态，支持启动/停止/接管开关/打开目录 | 待 GUI 人工冒烟 |
| 关闭接管 | 新增 GUI `uninstall_clients_env` 和 CLI `env uninstall` | 待隔离 Windows 用户验证 |

### 3.6 CLI 功能

| 命令 | 测试情况 | 结论 |
| --- | --- | --- |
| `status` | 临时配置目录自动初始化并显示状态 | 通过 |
| `init` | 写入 provider，Base URL 规范化，Key 脱敏展示 | 通过 |
| `serve` | 启动后 `/health`、`/v1/models` 冒烟通过 | 通过 |
| `env status` | 输出客户端环境变量缺失情况 | 通过 |
| `env print` | 输出本地网关环境变量脚本，不修改系统 | 通过 |
| `test` | 需要真实上游 Key | 需人工确认 |

### 3.6 网关和协议适配

| 功能 | 测试情况 | 结论 |
| --- | --- | --- |
| `/health` | demo provider 下返回 `ok=true`、active model、service | 通过 |
| `/v1/models` | 返回 OpenAI list 结构，包含 active model | 通过 |
| OpenAI 转发 | 代码检查确认会重写 model 并携带 Bearer Key | 需真实 Key 验证 |
| Anthropic 原生转发 | 代码路径存在 | 需真实 Key 验证 |
| Anthropic -> OpenAI 适配 | Rust 单测通过 | 通过 |
| OpenAI SSE -> Anthropic SSE | Rust 单测通过 | 通过 |
| 故障转移 | 5xx 时尝试下一个 provider，4xx 直接返回 | 通过 |

## 4. 自动化与冒烟结果

| 序号 | 命令/动作 | 状态 | 关键结果 |
| ---: | --- | --- | --- |
| 1 | `npm run check` | ✅ | TypeScript 类型检查通过 |
| 2 | `npm run build` | ✅ | Vite 构建通过，生成 `dist/` |
| 3 | `cargo test` | ✅ | 14 passed，0 failed |
| 4 | `npm run package:release` | ✅ | portable 目录生成成功，包含 GUI 与 CLI |
| 5 | `scripts/package-release.ps1 -SkipBuild -Zip` | ✅ | 使用 .NET ZipFile 生成 zip 成功；锁定场景下可提示占用进程 PID/路径 |
| 6 | release CLI `status/init/env status/env print` | ✅ | 临时配置隔离，Key 脱敏，dummy key 输出正确 |
| 7 | release CLI `serve` + `/health` | ✅ | 返回 `ok=true` 与 `demo-model` |
| 8 | release CLI `serve` + `/v1/models` | ✅ | 返回 OpenAI list，包含 `demo-model` |
| 9 | `npm run package:release:zip` | ⚠️ | 构建成功；清理旧目录时 `SUGT.exe` 被进程锁定；脚本已能提示 PID/路径，需关闭 GUI 后复测完整入口 |

## 5. 测试点统计

| 分类 | 总数 | ✅ 通过 | ⚠️ 部分/需人工确认 | ⏭️ 未执行 | ❌ 不通过 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 构建与静态检查 | 6 | 5 | 1 | 0 | 0 |
| 配置与数据模型 | 6 | 5 | 1 | 0 | 0 |
| GUI 控制台 | 7 | 5 | 2 | 0 | 0 |
| GUI 模型配置 | 13 | 12 | 1 | 0 | 0 |
| GUI 客户端接管 | 5 | 3 | 1 | 1 | 0 |
| GUI 日志与关于 | 4 | 4 | 0 | 0 | 0 |
| CLI | 6 | 5 | 1 | 0 | 0 |
| 网关接口与协议适配 | 9 | 6 | 3 | 0 | 0 |
| 安全与隐私 | 5 | 4 | 1 | 0 | 0 |
| 界面可用性与体验 | 5 | 5 | 0 | 0 | 0 |
| 内部试用与托盘 | 9 | 0 | 0 | 9 | 0 |
| **合计** | **75** | **54** | **11** | **10** | **0** |

## 6. 本轮发现的问题与风险

### 6.1 npm zip 完整入口受进程锁定影响

**现象**：再次执行 `npm run package:release:zip` 时，前端、Tauri GUI、Cargo CLI release 构建均成功，但组包清理阶段失败：

```text
Remove-Item : 无法删除项 ...\release\SUGT-0.1.0-portable-windows-x64\SUGT.exe: 访问被拒绝
```

随后检查到系统中存在运行中的 `SUGT.exe` 进程，PID 为 `34624`。该进程很可能正在占用 release 目录下的旧 `SUGT.exe`，导致脚本无法删除旧产物。

**影响**：完整 zip 入口无法在旧 GUI 进程运行时覆盖组包目录。

**当前处理**：未擅自结束用户进程；已改进 `scripts/package-release.ps1`，清理失败前重试 3 次，失败后检测 release 输出目录中的 `SUGT.exe`/`sugt-cli.exe` 进程，并输出 PID 与路径。当前锁定场景复测已能提示 `SUGT.exe (PID 34624, path ...\release\SUGT-0.1.0-portable-windows-x64\SUGT.exe)`。

**建议**：

1. 关闭 `release\SUGT-0.1.0-portable-windows-x64\SUGT.exe` 后重新执行 `npm run package:release:zip`。
2. 保留当前脚本的重试和占用进程提示，不建议默认强杀进程。
3. 如确需支持自动关闭进程，应增加显式参数，例如 `-ForceStopRunningApp`。

### 6.2 zip 实现兼容性问题已修复

**现象**：PowerShell `Compress-Archive` 在当前环境下失败，退出码 2 且无详细错误。

**处理**：已将 `scripts/package-release.ps1` zip 逻辑改为 `.NET System.IO.Compression.ZipFile.CreateFromDirectory(...)`。

**验证**：`scripts/package-release.ps1 -SkipBuild -Zip` 已通过并生成 zip。

### 6.3 真实上游访问未验证

**原因**：本轮没有注入真实 API Key，避免泄露密钥、产生费用或访问外部服务失败导致误判。

**影响**：真实 OpenAI/Anthropic 上游 chat/messages、模型连接测试、CLI `test` 仍需人工或受控环境验证。

**建议**：增加环境变量驱动的可选集成测试，例如：

- `SUGT_TEST_BASE_URL`
- `SUGT_TEST_API_KEY`
- `SUGT_TEST_MODEL`
- `SUGT_TEST_PROTOCOL`

只有这些变量存在时才执行真实上游测试。

### 6.4 系统状态修改类功能未自动执行

**范围**：一键接管 `setx`、开机启动。

**原因**：这些功能会修改当前 Windows 用户环境变量或系统启动项。

**建议**：使用隔离 Windows 用户、虚拟机或 CI runner 做专项验证，并在测试后清理环境变量和启动项。

## 7. 业务评估

### 7.1 业务价值

SUGT 的业务目标清晰：解决研发工具接入国内/上游模型服务时协议、环境变量和模型配置不统一的问题。它将复杂的客户端配置收敛到本地 GUI/CLI，并通过本地代理提供统一接口，对内部研发提效价值明显。

### 7.2 业务闭环

当前已形成较完整闭环：

1. GUI/CLI 初始化和维护 provider。
2. 本地网关统一暴露兼容接口。
3. Claude/Codex 通过本地变量接入网关。
4. 网关按 active provider 转发或协议适配。
5. 日志和状态用于排查问题。
6. portable 包便于内部用户分发。

### 7.3 业务风险

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 上游连通性依赖外部服务 | 国内网络、模型服务状态、Key 权限都会影响用户体验 | 在 GUI 中提供更明确的连接诊断和错误分类 |
| 客户端环境变量接管影响全局开发环境 | `setx` 可能影响用户已有 Claude/Codex 配置 | 接管前展示变更预览，提供恢复/卸载功能 |
| 多 provider 故障转移语义需要更清晰 | 目前 5xx 重试、4xx 不重试合理，但用户可能不理解 | UI 上说明故障转移规则，并记录实际命中的 provider |
| 配置文件保存真实 API Key | 本地工具可接受，但需要用户知道风险 | 首次保存 Key 时提示本地明文配置文件路径和保护建议 |

## 8. 界面评估

### 8.1 界面优点

1. **信息架构清晰**：控制台、模型配置、客户端接管、运行日志、关于五个页面符合使用流程。
2. **暗色科技风统一**：样式围绕暗色背景、强调色和状态 badge，适合开发工具场景。
3. **操作反馈完整**：多数操作通过 notice/message 展示成功或失败。
4. **防重复点击**：`busy` 状态会禁用关键按钮，降低重复请求概率。
5. **列表可用性较好**：provider 列表和日志区均支持滚动，适配配置较多或日志较长的情况。
6. **响应式适配**：小窗口下有媒体查询，降低布局溢出风险。

### 8.2 可改进点

| 问题 | 影响 | 建议 |
| --- | --- | --- |
| 缺少 GUI 自动化回归 | 手工点击难以稳定复现 | 引入 Tauri Driver 或 Playwright 做最小 E2E |
| 连接测试失败原因可能不够细 | 用户难以区分网络、鉴权、模型名错误 | 解析 HTTP 状态码和错误体，给出分类提示 |
| 接管操作风险提示不足 | 可能覆盖用户已有环境变量 | 接管前弹窗展示将写入的变量、原值状态和恢复方式 |
| 当前运行 provider 命中情况不够可视 | 故障转移后用户可能不知道实际用了哪个 provider | 日志和控制台展示最近一次请求命中的 provider |
| 缺少配置导入/导出 | 多设备迁移不方便 | 增加脱敏导出和可选含 Key 的加密导出 |

## 9. 安全与隐私评估

### 9.1 已具备的安全措施

1. **API Key 脱敏展示**：前端视图和 CLI status 不显示完整 API Key。
2. **客户端 dummy key**：Claude/Codex 只需要访问本地网关，不持有真实上游 Key。
3. **日志不直接记录完整 Key**：代理日志以 provider/model/path/status 为主。
4. **4xx 不重试**：避免鉴权或参数错误在多个 provider 间扩散。
5. **临时测试使用 `SUGT_CONFIG_DIR`**：验证过程没有污染真实用户配置。

### 9.2 需要加强的安全点

| 项目 | 风险 | 建议 |
| --- | --- | --- |
| 本地 TOML 明文保存 API Key | 其他本机进程或用户可能读取 | 增加文件权限收紧、Windows Credential Manager 或可选加密存储 |
| CORS `Any` | 如果监听非本机地址，网页可跨域访问本地网关 | 默认只监听 `127.0.0.1`；非本机监听时提示风险，可配置 allowlist |
| 客户端接管全局环境变量 | 影响现有开发工具配置 | 已增加关闭接管入口；后续补充备份/恢复旧值能力 |
| 错误提示可能包含上游错误体 | 上游可能回显敏感内容 | 对日志和 UI 错误体做长度限制和敏感字段过滤 |
| 打包产物覆盖 | 旧进程运行时无法覆盖 | 检测锁定并输出安全提示，不默认强杀 |
| `-SkipBuild` 复用旧二进制元数据 | trial/self 包目录和 exe 内置元数据可能不一致 | 对 `-SkipBuild` + variant/有效期参数增加强警告或限制 |

## 10. 稳定性与发布评估

### 10.1 稳定性结论

从历史验证看，项目在类型检查、前端构建、Rust 单元测试、portable 打包、release CLI、网关基础接口方面稳定性较好。核心数据模型、协议转换、failover 和 URL 规范化均有单元测试覆盖。本轮新增的内部试用、机器绑定、trial/self 打包、关闭接管和托盘增强尚需完成编译、单测、前端构建和打包冒烟后再更新稳定性结论。

### 10.2 发布风险

当前主要发布风险不是代码编译失败，而是 Windows 运行态文件锁定导致组包目录清理失败。该风险常见于用户打开 release 目录中的 GUI 后重新打包。

发布脚本已改进为：

1. 清理 release 输出目录失败时等待并重试 3 次。
2. 检测运行路径位于目标 release 目录的 `SUGT.exe`、`sugt-cli.exe`。
3. 错误提示中输出占用进程的 PID 与路径。
4. 不默认强杀进程，避免误伤用户正在使用的应用。

仍需在用户关闭当前 portable GUI 后复测 `npm run package:release:zip` 完整入口。

新增 trial/self 发布链路还需关注：

1. `build.rs` 已监听 `SUGT_BUILD_ID`、`SUGT_EDITION`、`SUGT_TRIAL_ENABLED`、`SUGT_TRIAL_EXPIRES_AT`，避免正常构建时元数据不刷新。
2. `-SkipBuild` 会跳过重编译，存在复用旧二进制内置元数据风险，建议增加警告或限制。
3. trial 包需要验证到期、机器绑定、状态文件路径和关于页文案；self 包需要验证无有效期限制。

## 11. 优化加强建议

### 11.1 高优先级

| 建议 | 价值 |
| --- | --- |
| 完成 `cargo fmt`、`npm run check`、`npm run build`、`cargo test` | 确认本轮 trial/self/托盘增强没有编译或类型回归 |
| 分别冒烟 trial/self 包 | 确认有效期、自用无有效期、CLI status、状态文件路径和目录命名正确 |
| 关闭当前 portable GUI 后复测 `npm run package:release:zip` | 确认完整 npm zip 入口在无锁场景下通过 |
| 增加真实上游可选集成测试 | 验证 `test_provider`、CLI `test`、OpenAI/Anthropic 真实转发 |
| 增加接管前环境变量变更预览和恢复能力 | 降低覆盖用户开发环境的风险 |
| 增加配置文件安全提示或 Key 存储增强 | 提升内部工具安全基线 |
| 增加 GUI 最小 E2E：启动、添加 provider、切换页签、查看日志 | 防止前端回归 |

### 11.2 中优先级

| 建议 | 价值 |
| --- | --- |
| 为 `/health` 无 provider 场景补充自动化测试 | 覆盖首次启动/未配置状态 |
| 为非法 TOML 配置补充测试 | 防止配置损坏后体验不可控 |
| 日志中增加 request id/provider id/耗时 | 提升排障效率 |
| UI 展示最近一次请求状态和命中 provider | 让 failover 行为更透明 |
| 增加导入/导出配置 | 方便内部团队迁移和共享配置模板 |

### 11.3 低优先级

| 建议 | 价值 |
| --- | --- |
| 增加浅色主题或跟随系统主题 | 提升可访问性 |
| 增加模型配置搜索/筛选 | provider 数量多时更易管理 |
| 增加 README 的常见错误排查表 | 降低新用户使用门槛 |
| 增加 zip 包校验值输出 | 便于内部发布校验 |

## 12. 建议的后续验收清单

发布前建议按以下顺序完成最终确认：

1. 执行 `cargo fmt`、`npm run check`、`npm run build`、`cargo test`。
2. 分别执行 self/trial CLI 或完整包冒烟，确认：
   - self 包显示无有效期限制；
   - trial 包显示有效期；
   - trial 首次运行状态文件写入 `Documents/.sugt` 或 `SUGT_CONFIG_DIR`；
   - exe 所在目录不生成运行时状态文件。
3. 关闭当前运行中的旧 release GUI 后重新执行 `npm run package:release:zip` 或 `npm run package:release:all:zip`，确认完整入口通过。
4. 使用临时真实 API Key 执行：
   - GUI 模型连接测试；
   - CLI `sugt-cli test`；
   - OpenAI chat completions 转发；
   - Anthropic messages 或 Anthropic-to-OpenAI 适配转发。
5. 在隔离 Windows 用户或虚拟机执行：
   - 一键接管；
   - `env status` 验证接管成功；
   - `env uninstall` 或 GUI 关闭接管；
   - 客户端实际调用；
   - 恢复/清理环境变量。
6. 手工点击 GUI 五个页面和托盘菜单，确认窗口缩放、弹窗、滚动、错误提示、启动/停止网关、打开配置目录体验。

## 13. 最终结论

SUGT 当前已具备较完整的内部研发本地大模型网关能力，并已扩展内部试用治理与托盘运维能力。测试点已从 65 个扩展为 75 个，其中历史验证 54 个通过、11 个因真实上游、系统状态修改、GUI 点击自动化或运行中进程锁定而标记为部分通过/需人工确认；新增 10 个 trial/self/托盘/关闭接管相关测试点待执行。

从历史测试结果看，项目核心功能链路可用：配置初始化、模型配置、默认模型标识/切换、列表级启禁用、一键测试、Key 脱敏、CLI、网关健康检查、模型列表、协议适配、failover、portable 打包均表现正常。本轮新增的试用有效期、机器绑定、自用无有效期、关闭接管、托盘增强和 trial/self 打包入口需要先完成格式化、类型检查、构建、Rust 单测与打包冒烟，再作为发布结论依据。
