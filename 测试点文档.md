# SUGT 项目测试点文档

## 1. 项目概述

SUGT（su gateway）是面向内部研发场景的本地大模型网关工具，核心目标是让 Claude Code、Codex 等客户端通过本地 OpenAI/Anthropic 兼容代理访问上游大模型服务。项目形态包括：

- GUI：Tauri 2 + React/TypeScript 控制台。
- 后端：Rust + Axum 本地网关。
- CLI：`sugt-cli` 命令行工具。
- 配置：本机 `Documents/.sugt/config.toml` 或 `SUGT_CONFIG_DIR` 指定目录。
- 发布：portable 绿色包，包含 `SUGT.exe` 和 `sugt-cli.exe`。

## 2. 测试范围

| 范围 | 覆盖内容 |
| --- | --- |
| 业务功能 | 本地网关启动停止、模型配置、协议适配、故障转移、客户端接管、日志查看 |
| 界面体验 | 导航、控制台状态、表单校验、弹窗、列表、滚动、错误提示、响应式布局 |
| CLI | `init`、`status`、`serve`、`test`、`env` 子命令 |
| 网关接口 | `/health`、`/v1/models`、OpenAI 代理、Anthropic Messages 代理 |
| 配置与安全 | 配置初始化、API Key 脱敏、环境变量接管、日志敏感信息控制 |
| 构建发布 | TypeScript 检查、Rust 单测、前端构建、GUI/CLI 一次打包、试用版/自用版打包 |
| 内部试用治理 | 编译期有效期、机器绑定、到期限制、自用无有效期、关于页低调展示 |
| 系统托盘 | 服务状态、模型状态、接管状态、启动停止、配置目录、接管开关 |
| 兼容性 | Windows PowerShell、WebView2、portable 包目录结构 |

## 3. 测试环境建议

| 项目 | 建议 |
| --- | --- |
| 系统 | Windows 10/11 x64 |
| Node.js | 与当前 `package-lock.json` 匹配版本，执行 `npm install` 后测试 |
| Rust | 满足 `src-tauri/Cargo.toml` 中 `rust-version = "1.78"` |
| Visual Studio Build Tools | MSVC x64 工具链可用 |
| WebView2 | GUI 运行环境可用 |
| 网络 | 可访问上游模型服务时执行真实连通性测试 |
| 隔离配置 | 自动化/回归测试建议使用临时 `SUGT_CONFIG_DIR`，避免污染真实用户配置 |

## 4. 测试点清单

### 4.1 构建与静态检查

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| BUILD-001 | TypeScript 类型检查 | 已安装 npm 依赖 | 执行 `npm run check` | `tsc --noEmit` 和 `tsconfig.node.json` 检查通过 | 高 |
| BUILD-002 | 前端生产构建 | 已安装 npm 依赖 | 执行 `npm run build` | 生成 `dist/index.html` 和 assets，无构建错误 | 高 |
| BUILD-003 | Rust 单元测试 | Rust/MSVC 环境可用 | 在 `src-tauri` 执行 `cargo test` | 所有 Rust 单元测试通过 | 高 |
| BUILD-004 | GUI + CLI portable 打包 | Node/Rust/MSVC 环境可用 | 执行 `npm run package:release` | `release/SUGT-{version}-portable-windows-x64` 同时包含 `SUGT.exe` 与 `sugt-cli.exe` | 高 |
| BUILD-005 | SkipBuild 复用产物组包 | 已存在 release 二进制 | 执行 `scripts/package-release.ps1 -SkipBuild` | 不重新编译即可重新生成 portable 包清单 | 中 |
| BUILD-006 | Zip 包生成 | portable 组包可用 | 执行 `npm run package:release:zip` | 生成 `release/SUGT-{version}-portable-windows-x64.zip` | 中 |

### 4.2 配置与数据模型

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| CFG-001 | 首次启动配置初始化 | 临时配置目录为空 | 执行 CLI `status` 或启动 GUI | 自动创建 `config.toml` 和配置目录 | 高 |
| CFG-002 | 默认监听地址 | 使用默认配置 | 查看状态 | `host=127.0.0.1`、`port=8787` | 高 |
| CFG-003 | OpenAI Base URL 规范化 | 输入不带 `/v1` 的 OpenAI URL | 保存模型配置 | 配置保存为以 `/v1` 结尾 | 高 |
| CFG-004 | Anthropic Base URL 规范化 | 输入 Anthropic URL | 保存模型配置 | 不强制追加 `/v1` | 高 |
| CFG-005 | API Key 脱敏展示 | 存在模型配置 | 查看模型列表或 CLI status | UI/CLI 不展示完整 API Key | 高 |
| CFG-006 | 配置读写容错 | 配置 TOML 非法 | 启动或读取配置 | 返回明确错误，不静默覆盖用户配置 | 中 |

### 4.3 GUI 控制台

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| GUI-001 | 应用主界面加载 | GUI 可启动 | 打开 `SUGT.exe` | 显示侧边栏、控制台、模型配置、客户端、日志、关于页签 | 高 |
| GUI-002 | 服务状态展示 | 默认配置 | 进入控制台 | 展示运行中/已停止、监听地址、当前服务商、当前模型 | 高 |
| GUI-003 | 启动网关 | 存在有效模型配置或允许无模型启动 | 点击“启动” | 服务启动，按钮状态刷新，`/health` 可访问 | 高 |
| GUI-004 | 停止网关 | 网关运行中 | 点击“停止” | 服务停止，状态变为已停止 | 高 |
| GUI-005 | 配置目录按钮 | GUI 可启动 | 点击“配置目录” | 打开配置目录，不阻塞界面 | 中 |
| GUI-006 | 开机启动开关 | Windows 环境 | 切换“开机启动” | 注册表/配置状态更新，失败时有提示 | 中 |
| GUI-007 | 故障转移开关 | GUI 可启动 | 切换“故障转移” | 配置持久化，网关运行配置刷新 | 高 |

### 4.4 GUI 模型配置

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| MODEL-001 | 添加模型表单 | 进入模型配置页 | 点击“添加模型” | 弹窗显示名称、服务商、协议、Base URL、API Key、Model Name、启用项 | 高 |
| MODEL-002 | 魔搭 OpenAI 预设 | 添加模型弹窗 | 点击“魔搭 OpenAI” | 自动填充 OpenAI 兼容 Base URL、模型名、协议 | 高 |
| MODEL-003 | 魔搭 Anthropic 预设 | 添加模型弹窗 | 点击“魔搭 Anthropic” | 自动填充 Anthropic Base URL、模型名、协议 | 高 |
| MODEL-004 | 新增模型必填校验 | 表单缺少必填字段 | 点击保存 | 阻止保存并提示字段不能为空/API Key 必填 | 高 |
| MODEL-005 | 编辑模型不强制重填 Key | 已有模型 | 编辑后 API Key 保持脱敏值或留空保存 | 不覆盖原 API Key | 高 |
| MODEL-006 | 删除模型 | 已有模型 | 点击删除并确认 | 模型从列表移除，active provider 自动切换 | 高 |
| MODEL-007 | 切换当前模型(设为默认) | 至少两个模型 | 点击“设为默认” | 当前模型高亮，显示"默认使用"标识，状态刷新 | 高 |
| MODEL-008 | 测试模型连接 | 有真实上游 Key | 点击“测试” | 根据真实 chat/messages 可用性标记 Available/Unavailable | 中 |
| MODEL-009 | 一键测试全部 | 有启用模型 | 点击"一键测试" | 逐个测试所有模型，汇总显示可用/不可用数量 | 中 |
| MODEL-010 | 列表级启用/禁用 | 有模型配置 | 点击"启用"/"禁用"按钮 | 列表刷新，禁用卡片置灰，已禁用模型不参与网关转发 | 高 |
| MODEL-011 | 禁用当前默认模型自动切换 | 默认模型被禁用 | 禁用正在使用的模型 | 自动切换到下一个启用模型；如无可用模型则默认为空 | 高 |
| MODEL-012 | 未启用模型不能设为默认 | 某模型已禁用 | 点击禁用模型的"设为默认" | 按钮不可点击或操作被拒绝 | 中 |
| MODEL-013 | 模型列表滚动 | 模型数量较多 | 添加多个模型查看列表 | 列表可滚动，操作按钮不被遮挡 | 低 |

### 4.5 GUI 客户端接管

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| CLIENT-001 | 接管状态展示 | 进入客户端页 | 查看 Claude/Codex 卡片 | 展示已接管/未接管与说明 | 高 |
| CLIENT-002 | 一键接管 | Windows 环境 | 点击“一键接管” | 写入用户环境变量并生成启动脚本 | 高 |
| CLIENT-003 | 接管后状态刷新 | 已执行一键接管 | 刷新页面或重新打开 | Claude/Codex 显示已接管 | 中 |
| CLIENT-004 | 启动脚本内容 | 执行接管 | 查看 `claude-sugt.cmd`、`codex-sugt.cmd` | 指向本地 SUGT 地址，不写入真实上游 Key | 高 |

### 4.6 GUI 日志与关于

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| LOG-001 | 日志为空态 | 日志文件不存在或为空 | 打开日志页 | 显示“暂无日志” | 中 |
| LOG-002 | 日志读取 | 网关启动/请求后 | 打开日志页点击刷新 | 显示最新日志，最多读取 500 行 | 高 |
| LOG-003 | 日志滚动 | 日志较多 | 查看日志页 | 日志区域可滚动，不撑破窗口 | 中 |
| ABOUT-001 | 关于信息 | GUI 可启动 | 打开关于页 | 展示产品名、作者、配置目录、日志文件 | 中 |

### 4.7 CLI

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| CLI-001 | `sugt-cli status` | CLI 可执行 | 执行 `sugt-cli.exe status` | 输出配置文件、监听地址、failover、模型列表 | 高 |
| CLI-002 | `sugt-cli init` | 临时配置目录 | 执行带 base-url/api-key/model 的 init | 写入模型配置并输出配置路径 | 高 |
| CLI-003 | `sugt-cli env status` | CLI 可执行 | 执行 `env status` | 输出 Claude/Codex 接管变量状态 | 高 |
| CLI-004 | `sugt-cli env print` | CLI 可执行 | 执行 `env print` | 输出临时环境变量脚本，不修改系统环境 | 中 |
| CLI-005 | `sugt-cli serve` | 有配置 | 指定 host/port 启动 | 监听指定地址，Ctrl+C 后退出 | 中 |
| CLI-006 | `sugt-cli test` | 有真实上游配置 | 执行 `test` | 输出每个 provider 连接状态 | 中 |

### 4.8 网关接口与协议适配

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| GW-001 | `/health` 无模型响应 | 网关运行，无 provider | 请求 `/health` | 返回 JSON，`ok=false` | 高 |
| GW-002 | `/health` 有模型响应 | 网关运行，有 provider | 请求 `/health` | 返回 `ok=true` 与 active model | 高 |
| GW-003 | `/v1/models` | 网关运行，有启用模型 | 请求 `/v1/models` | 返回 OpenAI 模型列表结构 | 高 |
| GW-004 | OpenAI 请求转发 | 有真实上游 | 请求 `/v1/chat/completions` | 上游收到重写后的 model 与鉴权头 | 高 |
| GW-005 | Anthropic Messages 原生转发 | Anthropic provider 可用 | 请求 `/v1/messages` | 使用 `x-api-key` 与 `anthropic-version` 转发 | 中 |
| GW-006 | Anthropic 到 OpenAI 适配 | OpenAI provider 可用 | 用 Anthropic 格式请求 `/v1/messages` | 转为 OpenAI chat，再转回 Anthropic 响应 | 高 |
| GW-007 | SSE 流式适配 | 上游支持 stream | 发起 stream 请求 | OpenAI SSE 转为 Anthropic SSE 事件序列 | 中 |
| GW-008 | 故障转移 | 多 provider 且 failover=true | 第一 provider 返回 5xx | 自动尝试后续 provider | 高 |
| GW-009 | 4xx 不重试 | provider 返回 400/401/403/404 | 发起请求 | 直接返回 4xx，避免错误切换掩盖鉴权/参数问题 | 中 |

### 4.9 安全与隐私

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| SEC-001 | API Key 不进入前端明文 | 有配置 | 查看 UI 模型列表 | 只显示脱敏 Key | 高 |
| SEC-002 | API Key 不进入客户端环境变量 | 执行一键接管 | 查看 Claude/Codex 环境变量 | 仅使用本地 dummy key，不暴露真实上游 Key | 高 |
| SEC-003 | 日志敏感信息 | 发起代理请求 | 查看日志 | 不记录完整 API Key | 高 |
| SEC-004 | 配置文件权限风险 | 配置生成后 | 检查保存位置与内容 | 明确 Key 保存在本机配置，不进入仓库 | 中 |
| SEC-005 | CORS 策略风险 | 网关运行 | 检查 CORS 配置 | 当前允许 Any，需评估内网使用风险 | 中 |

### 4.10 界面可用性与体验

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| UX-001 | 关键操作反馈 | 执行启动、停止、保存、删除 | 观察提示条 | 成功/失败提示明确 | 高 |
| UX-002 | Busy 状态防重复点击 | 执行异步操作 | 连续点击按钮 | 按钮禁用，避免重复提交 | 中 |
| UX-003 | 错误提示可理解 | 制造非法 URL/缺字段/端口占用 | 观察提示 | 错误信息能指导用户修复 | 高 |
| UX-004 | 小窗口布局 | 调整窗口接近最小尺寸 | 查看各页 | 内容不明显溢出，按钮可操作 | 中 |
| UX-005 | 暗色主题可读性 | 正常使用 | 观察对比度和字号 | 文字、按钮、状态标签清晰 | 中 |

### 4.11 内部试用版与托盘

| ID | 测试点 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| TRIAL-001 | 试用版有效期展示 | trial 包 | 打开关于页 | 显示“内部试用版本，有效期至 yyyy-MM-dd” | 高 |
| TRIAL-002 | 自用版无有效期展示 | self 包 | 打开关于页 | 显示“内部自用版本，无有效期限制” | 高 |
| TRIAL-003 | 到期禁止启动网关 | 已过期 trial 包 | 点击启动或执行 `sugt-cli serve` | 阻止启动并提示联系作者获取新版 | 高 |
| TRIAL-004 | 机器绑定状态写入 | trial 包首次运行 | 启动网关或执行需校验命令 | `Documents/.sugt/trial-state.json` 记录 build_id、machine_hash、时间信息 | 高 |
| TRIAL-005 | exe 目录不写运行时文件 | portable 包 | 运行 GUI/CLI 后检查 exe 所在目录 | 不生成运行时状态文件，状态文件仅在 `.sugt` | 高 |
| TRAY-001 | 托盘状态展示 | GUI 运行 | 打开托盘菜单 | 展示服务、模型、接管、版本状态 | 中 |
| TRAY-002 | 托盘启动/停止网关 | GUI 运行 | 从托盘点击启动/停止 | 网关状态切换，已到期时启动禁用 | 高 |
| TRAY-003 | 托盘打开配置目录 | GUI 运行 | 从托盘点击打开配置目录 | 打开 `Documents/.sugt` 或 `SUGT_CONFIG_DIR` | 中 |
| TRAY-004 | 托盘接管开关 | 隔离 Windows 用户 | 从托盘开启/关闭接管 | 开启写入 SUGT 环境变量，关闭仅移除 SUGT 管理的变量 | 中 |
| CLIENT-005 | 关闭接管 | 已接管环境 | 点击客户端页“关闭接管”或执行 `sugt-cli env uninstall` | 清理当前值等于 SUGT 期望值的用户环境变量 | 中 |

## 5. 回归测试建议

每次发布前建议至少执行：

1. `npm run check`
2. `npm run build`
3. `cargo test`（在 `src-tauri`）
4. `npm run package:release:self`
5. `npm run package:release:trial`
6. `scripts/package-release.ps1 -SkipBuild -Variant self`
7. portable 包内 `sugt-cli.exe status`
8. GUI 冒烟：打开 GUI、切换页签、添加/编辑模型表单、查看日志页、查看关于页有效期说明
9. 如有真实 API Key：执行 provider 连接测试、`/health`、`/v1/models`、一次 chat 请求

## 6. 暂不自动化的测试

| 测试项 | 原因 | 建议方式 |
| --- | --- | --- |
| 真实上游 chat 质量 | 依赖外部 API Key、网络、模型额度 | 发布前人工执行或 CI 注入密钥 |
| GUI 点击路径 | 当前未引入前端 E2E 框架 | 后续可引入 Playwright/Tauri driver |
| Windows 开机启动注册表 | 会修改用户系统状态 | 手工验证或隔离测试账号验证 |
| `setx` 用户环境变量写入 | 会污染真实用户环境 | 默认只做 `env print/status`，需要时手工确认 |
