# SUGT 开发测试报告

## 项目信息

- 软件名：SUGT
- 全称：su gateway
- 作者：孙文龙
- 定制方：QM 科技
- 当前版本：0.1.0
- 架构：Tauri 2 + Rust + React/TypeScript + Axum gateway
- 交付目录：`release/SUGT-0.1.0-portable-windows-x64`

## 已实现功能

- 本地 OpenAI-compatible gateway：`http://127.0.0.1:8787`
- 支持 `/health`、`/v1/models`、`/v1/chat/completions` 等 OpenAI 标准路径代理
- 请求转发时自动替换上游 `Authorization`，并将请求体 `model` 重写为当前模型配置
- 多模型配置、启用状态、当前模型选择、故障转移策略
- 配置与日志自动初始化到系统文档目录：`Documents/.sugt`
- GUI 三页签：控制台、模型配置、关于
- 托盘运行：隐藏任务栏、关闭/最小化到托盘、托盘启动/停止/退出
- CLI：`init`、`status`、`serve`、`test`
- Windows 开机启动注册表配置能力
- 极客风图标资源

## 本机敏感配置

- 配置文件：`C:\Users\swl\Documents\.sugt\config.toml`
- 日志文件：`C:\Users\swl\Documents\.sugt\sugt.log`
- API Key 仅保存在本机配置文件，未写入仓库；CLI/UI 展示时脱敏。

## 验证结果

| 项目 | 命令/方式 | 结果 |
| --- | --- | --- |
| Rust 编译检查 | `cargo check` | 通过 |
| Rust 单元测试 | `cargo test` | 通过，4 个测试全部通过 |
| 前端类型检查 | `npm run check` | 通过 |
| 前端生产构建 | `npm run build` | 通过 |
| CLI release 构建 | `cargo build --release --bin sugt-cli` | 通过 |
| GUI release 构建 | `cargo build --release --bin sugt` / `tauri build` 编译阶段 | 通过，生成 `sugt.exe` |
| portable CLI 验证 | `release/.../sugt-cli.exe status` | 通过 |
| 本地网关 `/health` | `GET http://127.0.0.1:8787/health` | 通过 |
| 本地网关 `/v1/models` | `GET http://127.0.0.1:8787/v1/models` | 通过 |
| 真实 provider chat 测试 | `sugt-cli.exe test` | 外部上游 chat 返回 500；程序正确标记 `Unavailable` |

## 真实业务测试结论

- 本地 gateway 已能启动并响应健康检查、模型列表。
- 已修复 OpenAI 本地路径到上游 `base_url` 的映射问题，避免将 `/v1/chat/completions` 错误转发为 `/v1/v1/chat/completions`。
- 使用用户提供的 `https://llm.nnrc.ren/v1` 做真实 chat 测试时，上游返回：
  - `500 Internal Server Error`
  - `code: do_request_failed`
- 对照结果说明：SUGT 本地路由、模型重写、鉴权转发链路已工作；当前 chat 业务不可用来自上游模型服务自身。`/models` 可访问但不能代表 chat 可用，因此已将“测试连接”改为以真实 `chat/completions` 探测为准。

## 打包说明

- 已生成可直接运行的 GUI exe：`src-tauri/target/release/sugt.exe`
- 已生成可直接运行的 CLI exe：`src-tauri/target/release/sugt-cli.exe`
- 已整理 portable 交付目录：`release/SUGT-0.1.0-portable-windows-x64`
  - `SUGT.exe`
  - `sugt-cli.exe`
  - `README.md`
  - `SUGT.ico`
- MSI/NSIS 安装包阶段因官方打包工具下载超时失败：
  - WiX：`https://github.com/wixtoolset/wix3/...`
  - NSIS：`https://github.com/tauri-apps/binary-releases/...`
- 当前 portable exe 不依赖开发环境，可作为轻量交付版运行。若需要安装包，可在网络稳定或预置 WiX/NSIS 后重跑 `npm run tauri -- build --bundles nsis`。

## 后续建议

1. 更换或修复上游模型服务，确保 `/v1/chat/completions` 可用。
2. 预置 NSIS/WiX 离线包后补齐安装器产物。
3. 后续可增加 Anthropic Messages API 兼容适配层，让 Claude Code 非 OpenAI 模式也能直接使用。
