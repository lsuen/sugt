# SUGT 测试点执行记录

> 本文档由 `测试点文档.md` 复制并扩展，用于记录 2026-06-10 本轮实际测试状态。状态说明：✅ 通过；⚠️ 部分通过/需人工确认；⏭️ 未执行；❌ 不通过。

## 1. 本轮测试环境

| 项目 | 实际情况 |
| --- | --- |
| 操作系统 | Windows win32 |
| 项目目录 | `D:\CodeFiles\RustProjects\sugt` |
| 前端 | React + TypeScript + Vite |
| 桌面端 | Tauri 2 |
| 后端 | Rust + Axum |
| 测试日期 | 2026-06-10 |
| 配置隔离 | CLI/网关测试使用 `%TEMP%\sugt-qwen-test`、`%TEMP%\sugt-qwen-gateway-test`，未污染真实用户配置 |
| 未执行项原则 | 不擅自写入用户环境变量、不擅自修改注册表、不擅自结束正在运行的 `SUGT.exe` |

## 2. 测试状态总览

| 分类 | 总数 | ✅ 通过 | ⚠️ 部分/需人工确认 | ⏭️ 未执行 | ❌ 不通过 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 构建与静态检查 | 6 | 5 | 1 | 0 | 0 |
| 配置与数据模型 | 6 | 5 | 1 | 0 | 0 |
| GUI 控制台 | 7 | 5 | 2 | 0 | 0 |
| GUI 模型配置 | 13 | 12 | 1 | 0 | 0 |
| GUI 客户端接管 | 5 | 3 | 1 | 1 | 0 |
| GUI 日志与关于 | 4 | 4 | 0 | 0 | 0 |
| CLI | 6 | 5 | 1 | 0 | 0 |
| 网关接口与协议适配 | 9 | 6 | 3 | 0 | 0 |
| 安全与隐私 | 5 | 4 | 1 | 0 | 0 |
| 界面可用性与体验 | 5 | 5 | 0 | 0 | 0 |
| 内部试用与托盘 | 9 | 0 | 0 | 9 | 0 |
| **合计** | **75** | **54** | **11** | **10** | **0** |

## 3. 自动化/命令执行结果

| 命令/方式 | 结果 | 备注 |
| --- | --- | --- |
| `npm run check` | ✅ 通过 | TypeScript 与 Vite 配置类型检查通过 |
| `npm run build` | ✅ 通过 | 生成 `dist/index.html` 与 assets |
| `cargo test`（`src-tauri`） | ✅ 通过 | 14 个 Rust 单元测试通过；存在 hard link warning，不影响测试结果 |
| `npm run package:release` | ✅ 通过 | 历史验证：同时生成 `SUGT.exe` 与 `sugt-cli.exe` portable 包；本轮已改为默认 self 包，待复测 |
| `scripts/package-release.ps1 -SkipBuild -Zip` | ✅ 通过 | 历史验证：修复 zip 逻辑后可生成 zip；锁定场景下已能提示占用进程 PID/路径 |
| `npm run package:release:zip` | ⚠️ 部分通过 | 历史验证：构建阶段通过，组包阶段因正在运行的 `release/.../SUGT.exe` 被锁定；本轮新 trial/self 入口待复测 |
| trial/self 打包入口 | ⏭️ 未执行 | 待执行 `package-release.ps1 -Variant self/trial -Target cli` 或对应 npm 入口 |
| release `sugt-cli.exe status/init/env status/env print` | ✅ 通过 | 历史验证：临时配置目录测试通过，API Key 脱敏展示；本轮新增 trial 状态与 env uninstall 待复测 |
| release `sugt-cli.exe serve` + `/health` + `/v1/models` | ✅ 通过 | 临时端口 `18787` 冒烟通过 |

## 4. 详细测试点执行表

### 4.1 构建与静态检查

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| BUILD-001 | TypeScript 类型检查 | `npm run check` | ✅ 通过 | `tsc --noEmit && tsc --noEmit -p tsconfig.node.json` 通过 |
| BUILD-002 | 前端生产构建 | `npm run build` | ✅ 通过 | Vite 构建通过，生成 `dist/index.html` 与 assets |
| BUILD-003 | Rust 单元测试 | `cargo test` | ✅ 通过 | 14 个 Rust 单元测试通过；hard link warning 不影响结果 |
| BUILD-004 | GUI + CLI portable 打包 | `npm run package:release` | ✅ 通过 | `SUGT.exe` 约 16.51 MB，`sugt-cli.exe` 约 7.25 MB |
| BUILD-005 | SkipBuild 复用产物组包 | `scripts/package-release.ps1 -SkipBuild` / `-SkipBuild -Zip` | ✅ 通过 | 可复用已有 GUI/CLI 产物重新组包并生成 zip；锁定场景下可明确提示 PID/路径 |
| BUILD-006 | Zip 包生成 | `npm run package:release:zip` | ⚠️ 部分通过 | 脚本 zip 逻辑已修复；完整 npm 入口复测时因正在运行的 `SUGT.exe` 锁定旧产物，需关闭进程后复测 |

### 4.2 配置与数据模型

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| CFG-001 | 首次启动配置初始化 | 临时 `SUGT_CONFIG_DIR` + CLI status | ✅ 通过 | 自动创建 `%TEMP%\sugt-qwen-test\config.toml` |
| CFG-002 | 默认监听地址 | CLI status | ✅ 通过 | 输出 `listen: http://127.0.0.1:8787` |
| CFG-003 | OpenAI Base URL 规范化 | CLI init + Rust 单测 | ✅ 通过 | `https://example.com` 保存为 `https://example.com/v1` |
| CFG-004 | Anthropic Base URL 规范化 | Rust 单测/代码检查 | ✅ 通过 | Anthropic URL 不强制追加 `/v1` |
| CFG-005 | API Key 脱敏展示 | CLI status | ✅ 通过 | `test-secret-123456` 显示为 `test****3456` |
| CFG-006 | 配置读写容错 | 代码检查 | ⚠️ 需加强 | 读取非法 TOML 会返回错误；建议增加专门自动化用例，验证不会覆盖原文件 |

### 4.3 GUI 控制台

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| GUI-001 | 应用主界面加载 | 代码检查 + 打包产物 | ✅ 通过 | `main.tsx` 包含完整侧边栏与 5 个页签，`SUGT.exe` 可打包生成 |
| GUI-002 | 服务状态展示 | 代码检查 | ✅ 通过 | `get_status` 返回 running、listen_url、active_model、active_provider、config_dir、log_file |
| GUI-003 | 启动网关 | release CLI serve + `/health` | ✅ 通过 | 端口 `18787` 启动后 `/health` 返回 `ok=true` |
| GUI-004 | 停止网关 | 测试脚本 finally 停止进程 | ✅ 通过 | 临时 serve 测试结束后进程被停止；GUI 命令层 `stop_gateway` 存在 |
| GUI-005 | 配置目录按钮 | 代码检查 | ✅ 通过 | `open_config_dir` 按平台打开目录 |
| GUI-006 | 开机启动开关 | 代码检查，未修改注册表 | ⚠️ 需人工确认 | `set_autostart` 逻辑存在；因会改用户系统状态，未自动执行 |
| GUI-007 | 故障转移开关 | 代码检查 + Rust 单测 | ✅ 通过 | `orders_failover_providers` 单测通过，UI 可调用 `set_failover` |

### 4.4 GUI 模型配置

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| MODEL-001 | 添加模型表单 | 代码检查 | ✅ 通过 | 弹窗包含名称、服务商、协议、Base URL、API Key、Model Name、启用项 |
| MODEL-002 | 魔搭 OpenAI 预设 | 代码检查 | ✅ 通过 | 预设填充 `https://api-inference.modelscope.cn/v1`、OpenAI 协议和模型名 |
| MODEL-003 | 魔搭 Anthropic 预设 | 代码检查 | ✅ 通过 | 预设填充 `https://api-inference.modelscope.cn`、Anthropic 协议和模型名 |
| MODEL-004 | 新增模型必填校验 | Rust 命令层代码检查 | ✅ 通过 | `validate_provider_input` 校验必填字段与新增 API Key |
| MODEL-005 | 编辑模型不强制重填 Key | Rust 命令层代码检查 | ✅ 通过 | 编辑时空 Key 或脱敏 Key 不覆盖原 Key |
| MODEL-006 | 删除模型 | Rust 命令层代码检查 | ✅ 通过 | 删除 active provider 后自动切换到下一个 enabled provider |
| MODEL-007 | 切换当前模型(设为默认) | Rust 命令层/前端代码检查 | ✅ 通过 | `set_active_provider` 仅允许启用模型；前端显示“默认使用”并将按钮文案改为“设为默认/已默认” |
| MODEL-008 | 测试模型连接 | 需真实上游 Key | ⚠️ 需人工确认 | 本轮未注入真实 Key；代码会调用真实 chat/messages 探测并标记状态 |
| MODEL-009 | 一键测试全部 | 前端代码检查 | ✅ 通过 | `testAllProviders` 顺序调用 `test_provider` 并汇总可用/不可用数量 |
| MODEL-010 | 列表级启用/禁用 | Rust 命令层/前端代码检查 | ✅ 通过 | 新增 `set_provider_enabled`，列表按钮可快捷启用/禁用，禁用卡片置灰 |
| MODEL-011 | 禁用当前默认模型自动切换 | Rust 命令层代码检查 | ✅ 通过 | 禁用 active provider 时自动切换到下一个 enabled provider；没有可用模型则默认为空 |
| MODEL-012 | 未启用模型不能设为默认 | Rust 命令层/前端代码检查 | ✅ 通过 | 后端拒绝未启用模型设为默认，前端禁用对应按钮 |
| MODEL-013 | 模型列表滚动 | CSS 代码检查 | ✅ 通过 | `.provider-list-scroll` 限高并允许滚动，操作区支持换行 |

### 4.5 GUI 客户端接管

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| CLIENT-001 | 接管状态展示 | CLI env status | ✅ 通过 | 未接管时明确列出 missing 变量 |
| CLIENT-002 | 一键接管 | 代码检查，未执行 setx | ⚠️ 需人工确认 | `install_clients_env` 会写脚本并调用 `setx`；因会污染用户环境，未自动执行 |
| CLIENT-003 | 接管后状态刷新 | 代码检查 | ✅ 通过 | UI `run` 成功后调用 `refresh`，可刷新接管状态 |
| CLIENT-004 | 启动脚本内容 | CLI env print/代码检查 | ✅ 通过 | 输出本地 `sugt-local-key` dummy key，不暴露真实上游 Key |
| CLIENT-005 | 关闭接管 | 代码检查，未执行 reg delete | ⏭️ 未执行 | 新增 `uninstall_clients_env` 与 `sugt-cli env uninstall`；会删除用户环境变量，需隔离账号验证 |

### 4.6 GUI 日志与关于

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| LOG-001 | 日志为空态 | 代码检查 | ✅ 通过 | `logs.length ? ... : '暂无日志'` |
| LOG-002 | 日志读取 | 代码检查 | ✅ 通过 | `read_logs` 限制最多 500 行，前端默认读取 200 行 |
| LOG-003 | 日志滚动 | CSS 代码检查 | ✅ 通过 | 日志 `pre` 使用 `overflow: auto` |
| ABOUT-001 | 关于信息 | 代码检查 | ✅ 通过 | 展示产品、作者、配置目录、日志文件 |

### 4.7 CLI

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| CLI-001 | `sugt-cli status` | release CLI + 临时配置 | ✅ 通过 | 输出配置路径、监听地址、failover |
| CLI-002 | `sugt-cli init` | release CLI + 临时配置 | ✅ 通过 | 写入 provider 并输出配置路径 |
| CLI-003 | `sugt-cli env status` | release CLI + 临时配置 | ✅ 通过 | 输出 Claude/Codex configured=false 与 missing 列表 |
| CLI-004 | `sugt-cli env print` | release CLI + 临时配置 | ✅ 通过 | 输出临时环境变量脚本，不修改系统环境 |
| CLI-005 | `sugt-cli serve` | 临时配置 + health 请求 | ✅ 通过 | `http://127.0.0.1:18787` 可访问 |
| CLI-006 | `sugt-cli test` | 需真实上游 Key | ⚠️ 需人工确认 | 本轮未配置真实上游 Key，避免无效外部请求与泄露密钥 |

### 4.8 网关接口与协议适配

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| GW-001 | `/health` 无模型响应 | 代码检查 | ⚠️ 需补测 | 代码支持无 provider 返回 `ok=false`，本轮实际冒烟使用了 demo provider |
| GW-002 | `/health` 有模型响应 | 临时配置启动 serve | ✅ 通过 | 返回 `{"active_model":"demo-model","ok":true,"service":"SUGT"}` |
| GW-003 | `/v1/models` | 临时配置启动 serve | ✅ 通过 | 返回 OpenAI 模型列表结构，包含 `demo-model` |
| GW-004 | OpenAI 请求转发 | 需真实上游 Key | ⚠️ 需人工确认 | 未注入真实 Key；代码会重写 model 并转发 Authorization |
| GW-005 | Anthropic Messages 原生转发 | 需 Anthropic provider | ⚠️ 需人工确认 | 未注入真实 Anthropic provider；代码路径存在 |
| GW-006 | Anthropic 到 OpenAI 适配 | Rust 单测 | ✅ 通过 | `converts_anthropic_messages_to_openai_chat` 通过 |
| GW-007 | SSE 流式适配 | Rust 单测 | ✅ 通过 | `converts_openai_sse_stream_to_anthropic` 通过 |
| GW-008 | 故障转移 | Rust 单测/代码检查 | ✅ 通过 | `orders_failover_providers` 通过；5xx 才重试的逻辑存在 |
| GW-009 | 4xx 不重试 | 代码检查 | ✅ 通过 | `status().as_u16() < 500` 直接返回，避免掩盖鉴权/参数错误 |

### 4.9 安全与隐私

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| SEC-001 | API Key 不进入前端明文 | CLI status/代码检查 | ✅ 通过 | ProviderView 只返回 `api_key_masked` |
| SEC-002 | API Key 不进入客户端环境变量 | CLI env print/代码检查 | ✅ 通过 | 客户端环境变量使用 `sugt-local-key` |
| SEC-003 | 日志敏感信息 | 代码检查 | ✅ 通过 | 代理日志记录 provider/model/path/status，不记录完整 API Key |
| SEC-004 | 配置文件权限风险 | 代码检查 | ⚠️ 需加强 | Key 保存在本机 TOML；建议增加权限说明和首次启动安全提示 |
| SEC-005 | CORS 策略风险 | 代码检查 | ✅ 通过但需评估 | 当前 `CorsLayer` 允许 Any，内部工具可接受；若暴露到非本机网络需收紧 |

### 4.10 界面可用性与体验

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| UX-001 | 关键操作反馈 | 代码检查 | ✅ 通过 | `message` notice 统一反馈成功/失败 |
| UX-002 | Busy 状态防重复点击 | 代码检查 | ✅ 通过 | `busy` 状态会禁用关键按钮 |
| UX-003 | 错误提示可理解 | 代码检查/命令输出 | ✅ 通过 | 必填、URL、缺模型、监听失败等错误能显示给用户 |
| UX-004 | 小窗口布局 | CSS 代码检查 | ✅ 通过 | `@media (max-width: 900px)` 与最小窗口配置存在 |
| UX-005 | 暗色主题可读性 | CSS 代码检查 | ✅ 通过 | 主题色、状态 badge、按钮样式明确 |

### 4.11 内部试用版与托盘

| ID | 测试点 | 执行方式 | 状态 | 实际结果/备注 |
| --- | --- | --- | --- | --- |
| TRIAL-001 | 试用版有效期展示 | 待 trial 包冒烟 | ⏭️ 未执行 | 前端关于页已接入 `status.trial.message`；待打包验证 |
| TRIAL-002 | 自用版无有效期展示 | 待 self 包冒烟 | ⏭️ 未执行 | `SUGT_TRIAL_ENABLED=false` 时应显示自用无有效期；待打包验证 |
| TRIAL-003 | 到期禁止启动网关 | 待过期 trial 包 | ⏭️ 未执行 | `start_gateway`、CLI `serve/test` 已接入 `trial::ensure_allowed`；待过期包验证 |
| TRIAL-004 | 机器绑定状态写入 | 待 trial 包首次运行 | ⏭️ 未执行 | 状态文件应写入 `Documents/.sugt/trial-state.json` 或 `SUGT_CONFIG_DIR` |
| TRIAL-005 | exe 目录不写运行时文件 | 待 portable 包冒烟 | ⏭️ 未执行 | 运行状态文件设计不落 exe 目录；待产物目录检查 |
| TRAY-001 | 托盘状态展示 | 待 GUI 人工冒烟 | ⏭️ 未执行 | 托盘菜单已新增服务、模型、接管、版本状态项 |
| TRAY-002 | 托盘启动/停止网关 | 待 GUI 人工冒烟 | ⏭️ 未执行 | 托盘启动会检查 trial 状态；到期应禁用启动 |
| TRAY-003 | 托盘打开配置目录 | 待 GUI 人工冒烟 | ⏭️ 未执行 | 菜单项调用打开配置目录逻辑 |
| TRAY-004 | 托盘接管开关 | 隔离 Windows 用户 | ⏭️ 未执行 | 涉及用户环境变量写入/删除，需隔离账号确认 |

## 5. 本轮发现的问题/风险

| 编号 | 问题/风险 | 影响 | 当前处理 | 建议 |
| --- | --- | --- | --- | --- |
| RISK-001 | `npm run package:release:zip` 复测时旧 `SUGT.exe` 被运行中进程锁定 | 无法清理旧 portable 目录，导致完整 zip 入口失败 | 已改进脚本：清理失败前重试 3 次，随后检测并提示占用进程 PID/路径；未擅自结束用户进程 | 关闭 release 目录中的 `SUGT.exe` 后复测完整 npm zip 入口 |
| RISK-002 | `Compress-Archive` 在当前环境无详细错误、退出码 2 | zip 打包不稳定 | 已改为 .NET `ZipFile.CreateFromDirectory` | 保留 .NET 实现并后续复测 npm zip 入口 |
| RISK-003 | 真实上游 Key 未注入 | 无法验证真实 chat/messages 质量 | 未执行外部真实请求 | 发布前用 `SUGT_TEST_API_KEY` 或手工配置真实 provider 测试 |
| RISK-004 | GUI 未做自动点击 E2E | UI 只能代码级评估与产物级验证 | 已做代码检查与打包验证 | 后续引入 Playwright/Tauri Driver |
| RISK-005 | 一键接管和开机启动会改用户系统状态 | 自动执行可能污染环境 | 本轮只做代码检查 | 建议隔离 Windows 测试账号执行 |
| RISK-006 | `-SkipBuild` 可能复用上一次编译进 exe 的 trial/self 元数据 | 可能出现包目录标识与二进制内置有效期不一致 | 已记录为待修风险 | 建议脚本对 `-SkipBuild` + variant/有效期参数给出强警告或禁止 |
| RISK-007 | 本轮新增 trial/self/托盘代码尚未完成编译验证 | 可能存在 Rust/Tauri API 或 TS 类型问题 | 待执行 cargo fmt、npm check/build、cargo test | 验证失败后优先修复编译错误再做打包冒烟 |

## 6. 结论

本轮测试点已扩展至 75 个，新增覆盖内部试用有效期、首次运行机器绑定、自用无有效期、关于页低调展示、托盘状态/操作和关闭接管。历史自动化与代码级验证覆盖构建、单测、打包、CLI、网关健康检查、模型列表、模型默认/启禁用/一键测试、配置规范化、协议适配、安全脱敏和主要 UI 逻辑；本轮新增 trial/self/托盘能力仍需完成格式化、类型检查、构建、Rust 单测和打包冒烟后更新最终结果。
